[package]
name = "bitfield"
version = "0.0.0"
edition = "2021"
publish = false
autotests = false

[[test]]
name = "tests"
path = "tests/progress.rs"

[dependencies]
bitfield-impl = { path = "impl" }

[dev-dependencies]
trybuild = { version = "1.0.108", features = ["diff"] }
